doctype html
html(lang="en").h-100
  head
    title= Title
    script.
      var gon = !{JSON.stringify(gon)};
  body.h-100.p-4
    .container.h-100
      #chat.h-100
    script(src='/assets/main.js')
